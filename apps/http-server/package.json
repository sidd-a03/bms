{
  "name": "http-server",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "node --enable-source-maps dist/index.js",
    "build": "npm run typecheck && tsup src/index.ts --format esm --clean --dts --minify --sourcemap",
    "dev": "tsx watch src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/express": "^5.0.6",
    "@types/node": "^22.15.3",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "dotenv": "^17.3.1",
    "express": "^5.2.1"
  }
}
